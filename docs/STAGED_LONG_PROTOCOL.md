# 分阶段模型继续长训：预定协议

日期：2026-09-16。用户要求继续“先联合，再冻结脑内连接”的训练，检查成功率是否随更多读出训练提高。

## 训练设置

- 从上一轮三个种子 71/72/73 的分阶段模型出发：`results/plasticity/staged/main/staged-<seed>/final.pt`，已完成 262,144 总环境步数。
- 保留前 65,536 步联合学习得到的脑内权重，继续冻结全部 459,342 条允许连接。真实 MaleCNS 子图仍为 4,096 个神经元、1,314 个下降神经元读出；感觉编码、神经动态及动作残差映射沿用原工程建模。
- 只继续训练 actor/critic 的 3,960 个参数；保留 Adam 状态。每个种子新增 **786,432 步**，到达 **1,048,576 总步数**；三组共新增 **2,359,296 步**。
- 记录固定节点 **262,144 / 524,288 / 786,432 / 1,048,576**。按预定预算完成，不根据中间成绩调参、提前结束或选择模型。
- PPO 学习率 0.0003、8 环境、rollout 128、minibatch 64、每 rollout 4 轮优化、裁剪 0.2、GAE 0.95、折扣 0.99、奖励缩放 0.1、固定高斯标准差 0.4、梯度范数上限 0.5，均保持不变。采样与 PPO 更新代码与上一轮逐字一致。

## 完整状态续训

本轮来源检查点具有完整 rollout 状态，恢复模型、读出 Adam、环境与每环境 RNG、当前观测、脑状态、回合起点标记、Torch CPU/GPU RNG。保留上次未结束的回合，不重新初始化读出或重置脑状态。记录恢复后状态核验结果。

每个 rollout 检查冻结连接不变、没有梯度；最终同时与上一轮 262,144 步及原始联合 65,536 步模型核对脑内连接的字节内容。Adam 更新计数应从 16,384 到 65,536。

CUDA 的稀疏加法可能存在浮点执行顺序差异；沿用之前的非全局确定性设置，不承诺与一次不中断执行逐位相同。原有恢复与冻结测试已覆盖模型、优化器、环境、随机数；本轮新增一项整体恢复测试，检查随机动作、跨回合重置和下一次参数更新的一致性。真实图短流程只训练种子 71 的额外 1,024 步，使用地图 5,499,700–5,499,707，不纳入正式统计。

## 评估设计

- 验证地图：沿用 5,400,000–5,400,099，共 100 张。每个预定训练节点测试，展示趋势，不用于挑选模型。
- **新主要测试地图：5,700,000–5,700,199，共 200 张**。此前未使用。三个种子全部训练结束后，再统一测试四个预定节点的模型，观察预算增加后的变化。每个节点使用完全相同地图。
- 次要复测：最终 1,048,576 步模型在上一轮 5,600,000–5,600,199 地图评估，与已有 262,144 步结果（平均 80.7%）衔接。
- 报告逐种子与种子均值/样本标准差、成功/碰撞/超时、任务回报、角速度指令变化、训练耗时与显存。新测试只在训练全部结束后读取。
- 本轮仅比较该分阶段路线的预算变化，不对其他路线重新训练，也不跨不同地图直接比较成功率。

## 执行与产物

工作站 RTX 5090 工作站，物理 GPU 1，项目 Conda Python `${PROJECT_DIR}/.conda/bin/python`；不安装新依赖。

- 训练器：`scripts/extend_staged.py`；整体恢复：`flylab/continuation.py`。
- 本机目录：`${PROJECT_DIR}/results/plasticity/staged-long/`。
- 工作站目录：`${PROJECT_DIR}/results/plasticity/staged-long/`。
- 保留全部固定节点检查点、逐地图评估、源模型与源码哈希。旧模型和结果保留。

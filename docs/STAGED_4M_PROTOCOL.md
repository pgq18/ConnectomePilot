# 分阶段模型延长至 419 万步：预定协议

日期：2026-09-16。用户要求再训练更久，观察最高成功率。本协议在训练和新地图评估之前确定。

## 固定训练预算

- 三个训练种子 71、72、73，从 `results/plasticity/staged-long/main/staged-<seed>/final.pt` 的 **1,048,576 步**完整恢复，到 **4,194,304 总环境步**；每组新增 3,145,728 步，共新增 9,437,184 步。
- 前 65,536 步联合学习，之后保持 459,342 条允许脑内连接冻结；4,096 个神经元及 1,314 个下降神经元读出沿用已有 MaleCNS 子图。感觉编码、神经动态与小车动作映射是工程建模。
- 仅优化读出 actor/critic 的 3,960 个参数，继续同一 Adam 状态，更新计数由 65,536 到 262,144。完整恢复环境、神经状态、观测、回合标记、CPU/GPU RNG。
- 保持 PPO 学习率 0.0003、8 环境、rollout 128、minibatch 64、4 轮优化、裁剪 0.2、折扣 0.99、GAE 0.95、奖励缩放 0.1、标准差 0.4、梯度上限 0.5。采样和学习核心与上一轮逐字一致。
- 固定保存和评估节点：**1,048,576 / 1,572,864 / 2,097,152 / 2,621,440 / 3,145,728 / 3,670,016 / 4,194,304**。不依据中间成绩修改预算或参数。

## 验证选模与独立测试

- 本轮新的验证集：**5,410,000–5,410,199，200 张地图**。所有种子和节点使用完全相同地图，训练仍仅采样 20,000,000–20,999,999。
- 每个种子按验证成功次数选择一个检查点；并列时选择较早步数。另选一个跨种子的单模型，仍按验证成功次数、较早步数、较小种子号排序。
- **在读取任何新测试地图前**，将候选成绩、检查点哈希和选择结果写入 `selection.json`。新测试执行记录保存该文件的哈希和开始时间。
- 新测试集：**5,800,000–5,800,499，500 张地图**。全部训练结束并固定选择后，对七个预定节点统一评估，以展示曲线；测试集成绩不反过来选择模型。
- 分别报告最终 419 万步模型、每种子验证选出的模型，以及验证选出的单模型。若报告测试曲线的最高点，明确它属于事后描述，不能等同于独立选模成绩。
- 次要复测：起点、终点和验证选出的检查点，在上一轮 **5,700,000–5,700,199** 地图上比较。它仅用于衔接上一轮平均 85.8% 的结果。
- 展示逐种子成功率、均值/样本标准差、碰撞和超时、回报、角速度指令变化。对于选定单模型，给出 500 张地图成功比例的 Wilson 95% 区间；不把跨种子的重复地图当成 1,500 张独立地图。

## 执行、审计与解释

- RTX 5090 工作站 工作站仅使用物理 GPU 1；三个独立进程可并行共享该卡，每进程 4 个 CPU 线程。已检查有 24 个 CPU 线程、约 53 GiB 可用内存、GPU 1 空闲。其他卡工作负载不变。
- 使用项目 Conda `${PROJECT_DIR}/.conda/bin/python`，无需新依赖。预计串行训练约 81 分钟；并行实际速度在启动后测量。
- 延续已有 CUDA 非全局确定性设置，稀疏加法可能产生浮点顺序差异。恢复状态不意味着之后的 GPU 轨迹逐位一致；冻结权重必须与 65,536 步来源逐字节相同。
- 先做种子 71 的额外 1,024 步全流程检查，使用独立调试验证地图 5,499,800–5,499,807 和调试测试地图 5,899,800–5,899,807；不计入正式结果。
- 每个 rollout 检查脑内连接不变、无梯度、参数有限。保存所有固定节点、逐地图结果、恢复审计、Adam 计数、源文件和输入哈希。
- 本轮回答当前任务分布、模型与预算下观察到的成绩。有限训练和 500 张地图不能确定理论最高成功率，也不代表真实机器人性能。

产物：`results/plasticity/staged-4m/`；训练器：`scripts/extend_staged_4m.py`。
